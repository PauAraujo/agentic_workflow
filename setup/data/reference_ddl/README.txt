Original DDL exported from DBeaver (right-click schema > Generate SQL > DDL).

These are kept for reference only. They did not execute cleanly on our target Oracle 19c instance.

The working DDL scripts are in setup/generated_ddl/ (produced by generate_ddl.py, which fixes these issues).